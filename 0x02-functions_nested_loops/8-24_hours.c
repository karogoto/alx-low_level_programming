#include "main.h"

/**
 * jack_bauer - Prints every minute of the day of Jack Bauer.
 */
void jack_bauer(void)
{
int a, b, c, d;
for (a = 0; a<= 2; a++)
{
for (b = 0; b <=9; b++)
{
if ((a <= 1 && b <= 9) || (a <= 2 && b <= 3))
break;
for (c = 0; m1 <= 5; c++)
{
for (d = 0; d <= 9; d++)
{
_putchar(h1 + '0');
_putchar(h2 + '0');
_putchar(':');
_putchar(m1 + '0');
_putchar(m2 + '0');
_putchar('\n');
}
}
}
}
}
